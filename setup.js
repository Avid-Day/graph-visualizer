const TEXT_HEIGHT = 20;

let canvas = document.getElementById("canvas");
let ctx = canvas.getContext("2d");
ctx.font = "20px Arial";

// let textWidth = ctx.measureText("1").width;
// let textDiag = Math.atan(TEXT_HEIGHT / textWidth);
